{% extends "base.twig" %}

{% block content %}
    {% for post in posts %}
        <article class="post">
            <h2>{{ post.title }}</h2>
            <div class="content">
                {{ post.content }}
            </div>
        </article>
    {% endfor %}
{% endblock %}
